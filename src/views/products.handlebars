<h1>Products</h1>
<ul>
  {{#each products}}
    <li>
      <strong>{{title}}</strong>
      - ${{price}}
      <a href="/products/{{_id}}">View Details</a>
    </li>
  {{/each}}
</ul>
<p>Page {{page}} of {{totalPages}}</p>